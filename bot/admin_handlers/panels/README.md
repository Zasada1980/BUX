# Admin Panel Refactoring — Структура по кнопкам

## 📂 Новая структура

```
bot/
├── admin_panel_NEW.py              — Главный роутер (173 строки)
│
└── admin_handlers/
    └── panels/
        ├── __init__.py             — Экспорты модулей
        ├── main_panel.py           — 🚀 ОРБИТАЛЬНЫЙ ЦЕНТР (главная панель)
        ├── stats_panel.py          — 📊 Аналитика
        ├── refresh_panel.py        — 🔄 Перезагрузка систем
        ├── filters_panel.py        — 👷👨‍💼🔧❌ Фильтры
        │
        └── [TODO]:
            ├── users_panel.py      — 👥 Экипаж (список пользователей)
            ├── clients_panel.py    — 👔 Контрагенты
            └── schedule_panel.py   — 📅 Полетный план
```

## ✅ Реализованные модули

### 1. `main_panel.py` — Главная панель
**Обработчики:**
- `/admin` команда
- `admin:main` callback (возврат на главную)

**Функции:**
- `show_main_panel(message)` — отображение панели
- `get_main_panel_keyboard()` — клавиатура с 10 кнопками

**Логика:**
1. Проверка is_admin
2. GET /api/admin/users
3. Подсчёт метрик (total, active, по ролям)
4. Космический дизайн с emoji glow
5. 6 рядов кнопок

**Зависимости:**
- `bot.config`: is_admin, API_BASE_URL, INTERNAL_API_TOKEN
- `bot.api_client`: APIClient

---

### 2. `stats_panel.py` — Панель аналитики
**Обработчик:**
- `admin:stats` callback

**Функция:**
- `show_stats_panel(callback)` — отображение аналитики

**Логика:**
1. Проверка is_admin
2. Немедленный callback.answer()
3. GET /api/admin/users
4. Фильтрация активных workers
5. Подсчёт зарплат (total, avg)
6. Отображение разделов:
   - ТРУДОВЫЕ РЕСУРСЫ
   - СТАТУС СИСТЕМ

**API вызовы:**
- `GET /api/admin/users`

---

### 3. `refresh_panel.py` — Перезагрузка
**Обработчик:**
- `admin:refresh` callback

**Функция:**
- `handle_refresh(callback)` — обновление панели

**Логика:**
1. callback.answer("🔄 Обновляю...")
2. show_main_panel(callback.message)
3. Удаление старого сообщения

**Зависимости:**
- `panels.main_panel`: show_main_panel

---

### 4. `filters_panel.py` — Фильтры
**Обработчики:**
- `admin:filter:worker`
- `admin:filter:foreman`
- `admin:filter:admin`
- `admin:filter:inactive`

**Функция:**
- `handle_filter(callback)` — фильтрация пользователей

**Логика:**
1. Проверка is_admin
2. Немедленный callback.answer()
3. Парсинг filter_type из callback.data
4. GET /api/admin/users
5. Применение фильтра
6. Формирование списка с космическим дизайном
7. Кнопка "🚀 На мостик"

**Фильтры:**
- `worker` → role == 'worker', emoji ⚙️
- `foreman` → role == 'foreman', emoji 💫
- `admin` → role == 'admin', emoji 🌟
- `inactive` → active == 0, emoji 🧊

**API вызовы:**
- `GET /api/admin/users`

---

## 📋 TODO: Модули для создания

### 5. `users_panel.py` — Список пользователей
**Обработчик:**
- `admin:users:page:<N>` callback

**Функции:**
- `show_users_list(callback, page)` — список с пагинацией

**Логика:**
1. Проверка is_admin
2. GET /api/admin/users
3. Пагинация (10 на страницу)
4. Космический дизайн списка
5. Кнопки пагинации + детали пользователя

**Зависимости:**
- Текущий код в `bot/admin_handlers/admin_users.py` (перенести)

---

### 6. `clients_panel.py` — Контрагенты
**Обработчик:**
- `admin:clients` callback

**Функции:**
- `show_clients_panel(callback)` — список клиентов

**Логика:**
1. GET /api/admin/clients (TODO: создать endpoint)
2. Отображение списка
3. Кнопки управления

**Статус:** ⚠️ Заглушка (в разработке)

---

### 7. `schedule_panel.py` — Полетный план
**Обработчик:**
- `admin:schedule:view` callback

**Функции:**
- `show_schedule_panel(callback)` — календарь смен

**Логика:**
1. GET /api/admin/shifts (TODO: уточнить endpoint)
2. Отображение графика
3. Кнопки управления

**Статус:** ⚠️ Заглушка (в разработке)

---

## 🔄 Миграция на новую структуру

### Этап 1: Подготовка (DONE ✅)
- [x] Создать папку `bot/admin_handlers/panels/`
- [x] Создать 4 базовых модуля (main, stats, refresh, filters)
- [x] Создать `admin_panel_NEW.py` с импортами

### Этап 2: Тестирование (CURRENT)
- [ ] Локальный запуск с `admin_panel_NEW.py`
- [ ] Проверка всех 4 модулей
- [ ] Проверка callback.answer() timing

### Этап 3: Развёртывание
- [ ] Заменить `admin_panel.py` → `admin_panel_NEW.py` на сервере
- [ ] Перезапуск бота
- [ ] Проверка в Telegram

### Этап 4: Дополнительные модули
- [ ] Создать `users_panel.py` (перенести из admin_users.py)
- [ ] Создать `clients_panel.py`
- [ ] Создать `schedule_panel.py`

---

## 🎨 Дизайн соглашения

### Космическая тема
- **Разделители:** `━━━━━━━━━━━━━━━━━━━━━━━━━━━━━`
- **Дерево:** `├─`, `└─`, `│`
- **Glow emoji:** ✨ вокруг заголовков
- **Контекстные emoji:** ⚡🌟💫🧊⚙️👥💎📊✅🎯

### Кнопка возврата
Во всех панелях единообразная кнопка:
```python
InlineKeyboardButton(text="🚀 На мостик", callback_data="admin:main")
```

### Обработка ошибок
```python
try:
    # ... логика
except Exception as e:
    logger.error(f"<Module> error: {e}", exc_info=True)
    await callback.answer(f"❌ Ошибка: {e}", show_alert=True)
```

### callback.answer() timing
**КРИТИЧНО:** Вызывать СРАЗУ после проверки is_admin, ДО тяжёлых операций:
```python
if not is_admin(user_id):
    await callback.answer("❌ Доступ запрещён", show_alert=True)
    return

await callback.answer()  # ← ЗДЕСЬ, сразу

# Теперь тяжёлые операции
api = get_api()
users = await api.get_users()
```

---

## 📊 Статистика кода

### До рефакторинга
- `admin_panel.py`: **409 строк** — всё в одном файле

### После рефакторинга
- `admin_panel_NEW.py`: **173 строки** — только роутер
- `main_panel.py`: **154 строки** — главная панель
- `stats_panel.py`: **108 строк** — аналитика
- `refresh_panel.py`: **23 строки** — перезагрузка
- `filters_panel.py`: **133 строки** — фильтры

**Итого:** 591 строка (вместо 409), но с:
- ✅ Подробной документацией
- ✅ Чёткой структурой
- ✅ Лёгкостью расширения

---

## 🚀 Команды

### Тестирование импортов
```powershell
cd d:\TelegramOllama_ENV_DEMO\code
python -c "from bot.admin_handlers.panels import show_main_panel; print('OK')"
```

### Деплой на сервер
```powershell
# 1. Загрузить все файлы panels/
Get-ChildItem bot\admin_handlers\panels\*.py | ForEach-Object {
    Get-Content $_.FullName | ssh root@46.224.36.109 "cat > /tmp/$($_.Name)"
    ssh root@46.224.36.109 "docker cp /tmp/$($_.Name) prod_bot:/app/bot/admin_handlers/panels/$($_.Name)"
}

# 2. Заменить главный файл
Get-Content bot\admin_panel_NEW.py | ssh root@46.224.36.109 'cat > /tmp/admin_panel.py'
ssh root@46.224.36.109 'docker cp /tmp/admin_panel.py prod_bot:/app/bot/admin_panel.py'

# 3. Перезапуск
ssh root@46.224.36.109 'cd /opt/bux && docker-compose -f docker-compose.cloud.yml restart bot'
```

---

## 🔗 Связанные файлы

- `bot/admin_handlers/admin_users.py` — старый список пользователей (перенести в panels/)
- `bot/admin_handlers/admin_add_user.py` — FSM добавления (оставить как есть)
- `bot/admin_handlers/admin_reports.py` — отчёты (оставить как есть)
- `bot/admin_handlers/admin_salaries.py` — зарплаты (оставить как есть)
- `bot/config.py` — is_admin, API_BASE_URL, INTERNAL_API_TOKEN
- `bot/api_client.py` — APIClient для всех API вызовов
